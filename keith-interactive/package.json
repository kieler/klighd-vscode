{
  "name": "@kieler/keith-interactive",
  "version": "0.1.0",
  "description": "An extension for Theia to interctively apply constraints to the diagram",
  "author": "Kiel University https://rtsys.informatik.uni-kiel.de/kieler",
  "license": "EPL-1.0",
  "keywords": [
    "keith-extension",
    "theia",
    "interactive"
  ],
  "scripts": {
    "prepare": "yarn run clean && yarn run build",
    "clean": "rimraf lib",
    "lint": "tslint -c ../tslint.json --project ./tsconfig.json",
    "build": "tsc && yarn lint",
    "watch": "tsc -w"
  },
  "dependencies": {
    "sprotty": "0.9.0"
  },
  "files": [
    "lib",
    "src",
    "build"
  ]
}
