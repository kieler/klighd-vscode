{
  "name": "keith-sprotty",
  "version": "0.1.0",
  "description": "sprotty diagrams for Keith RiCharts",
  "author": "Kiel University <rt-kieler-devel@informatik.uni-kiel.de>",
  "license": "EPL-1.0",
  "keywords": [
    "sprotty",
    "keith",
    "diagram"
  ],
  "dependencies": {
    "sprotty": "^0.4.2"
  },
  "scripts": {
    "prepare": "yarn run clean && yarn build",
    "clean": "rimraf lib",
    "lint": "tslint -c ./tslint.json --project ./tsconfig.json",
    "build": "tsc",
    "watch": "tsc -w -p ./tsconfig.json"
  },
  "devDependencies": {
    "rimraf": "^2.6.1",
    "tslint": "^5.5.0",
    "typescript": "2.9.1"
  },
  "files": [
    "lib",
    "src",
    "build",
    "css"
  ]
}
